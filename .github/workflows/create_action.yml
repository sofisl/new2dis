name: conditional run
# This workflow is triggered on pushes to the repository.
on: 
  check_run:
    types: [completed]

jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: conditionalRun
        uses: sofisl/checkRunAction@v2
        with:
          conclusion: ${{ github.event.check_run.conclusion }}
          head_sha: ${{ github.sha }}
          author: ${{ github.actor }}
          myToken: ${{ github.token }}
   
