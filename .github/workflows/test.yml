name: Run a test
# This workflow is triggered on pushes to the repository.
on: pull_request

jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - run: npm i
      - run: npm test
      
